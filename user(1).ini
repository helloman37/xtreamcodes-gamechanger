; ---- uploads ----
upload_max_filesize=256M
post_max_size=256M

; ---- execution / memory ----
memory_limit=512M
max_execution_time=300
max_input_time=300

; ---- temp/session paths (optional) ----
; upload_tmp_dir=/home/youruser/tmp
; session.save_path=/ho